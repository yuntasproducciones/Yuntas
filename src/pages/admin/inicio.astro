---
import LayoutAdmin from "../../layouts/LayoutAdmin.astro";
import AdminDashboard from "../../components/admin/Admin";
const tiempoTranscurrido = Date.now();
const hoy = new Date(tiempoTranscurrido);
---

<LayoutAdmin title="VENTAS">
  <!-- Componente de autenticación que se ejecuta primero -->
  <AdminDashboard client:only="react" />
  <p class="text-2xl text-red-500 font-bold text-center mt-10">
    Bienvenido al Dashboard de Administrador. {hoy.toLocaleDateString()}
  </p>
  <div class="overflow-x-auto p-4">
    <table
      class="min-w-full text-sm text-center border-separate border-spacing-x-2 border-spacing-y-2"
    >
      <thead>
        <tr>
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >ID</th
          >
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >Nombre</th
          >
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >Gmail</th
          >
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >Teléfono</th
          >
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >Sección</th
          >
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >Fecha</th
          >
          <th
            class="px-4 py-2 bg-cyan-400 dark:bg-cyan-600 text-white uppercase text-xs font-bold rounded-md"
            >Acción</th
          >
        </tr>
      </thead>
      <tbody>
        <!-- Fila con datos impar (1) -->
        <tr>
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"
            >1</td
          >
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"
            >Kiara</td
          >
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"
            >namelose@gmail.com</td
          >
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"
            >941825478</td
          >
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"
            >WAOS</td
          >
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100"
            >25/01/2025</td
          >
          <td
            class="px-4 py-2 rounded-md font-bold bg-gray-200 dark:bg-gray-700 text-gray-900 dark:text-gray-100 flex justify-center gap-2"
          >
            <button
              class="text-red-500 hover:text-red-700 dark:text-red-400 dark:hover:text-red-300"
              >🗑️</button
            >
            <button
              class="text-green-500 hover:text-green-700 dark:text-green-400 dark:hover:text-green-300"
              >✅</button
            >
          </td>
        </tr>

        <!-- Filas vacías con estilo alternado -->
        {
          Array.from({ length: 9 }).map((_, i) => {
            const isEven = (i + 2) % 2 === 0;
            // Colores para modo claro y oscuro
            const lightBg = isEven ? "bg-gray-100" : "bg-gray-200";
            const darkBg = isEven ? "dark:bg-gray-800" : "dark:bg-gray-700";
            const textColor = "text-gray-900 dark:text-gray-100";
            const combinedClasses = `px-4 py-2 rounded-md font-bold ${lightBg} ${darkBg} ${textColor}`;

            return (
              <tr>
                <td class={combinedClasses}>{i + 2}</td>
                <td
                  class={`px-4 py-2 rounded-md ${lightBg} ${darkBg} ${textColor}`}
                />
                <td
                  class={`px-4 py-2 rounded-md ${lightBg} ${darkBg} ${textColor}`}
                />
                <td
                  class={`px-4 py-2 rounded-md ${lightBg} ${darkBg} ${textColor}`}
                />
                <td
                  class={`px-4 py-2 rounded-md ${lightBg} ${darkBg} ${textColor}`}
                />
                <td
                  class={`px-4 py-2 rounded-md ${lightBg} ${darkBg} ${textColor}`}
                />
                <td
                  class={`px-4 py-2 rounded-md ${lightBg} ${darkBg} ${textColor} flex justify-center gap-2`}
                >
                  <button class="text-red-400 dark:text-red-500 cursor-not-allowed opacity-50">
                    🗑️
                  </button>
                  <button class="text-green-400 dark:text-green-500 cursor-not-allowed opacity-50">
                    ✅
                  </button>
                </td>
              </tr>
            );
          })
        }
      </tbody>
    </table>
  </div>
</LayoutAdmin>
